{{- if .Values.appServerSf.enabled -}}
{{- if .Values.appServerSf.serviceAccount.create -}}
# Copyright Siemens 2024
apiVersion: v1
kind: ServiceAccount
metadata:
  name: app-server-sf
  labels:
    {{- include "opcenter-core.labels" . | nindent 4 }}
  {{- with .Values.appServerSf.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
